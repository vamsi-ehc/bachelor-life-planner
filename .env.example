# Punch In runs fully offline with no backend — no env vars are required to
# build or run the app. The only variable read at build time is below, and
# it only affects an SEO verification meta tag in index.html.

# Google Search Console verification token (optional)
VITE_GSC_VERIFICATION=
