# Offline build: no Firebase project, no Google sign-in, no network calls.
# Auth and all app data are stored entirely on-device in a client-side
# SQLite database (see src/localdb and src/auth/localAuth.ts).
VITE_AUTH_PROVIDER=local
